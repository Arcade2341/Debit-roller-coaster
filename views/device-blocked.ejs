<%- include("partials/head", { pageTitle, metaRobots: "noindex,nofollow" }) %>

<main class="status-page">
  <section class="container status-shell">
    <div class="form-shell">
      <span class="eyebrow"><%= t("status.deviceBlockedEyebrow") %></span>
      <h1><%= t("status.deviceBlockedTitle") %></h1>
      <p><%= t("status.deviceBlockedText") %></p>
    </div>
  </section>
</main>

<%- include("partials/footer") %>
